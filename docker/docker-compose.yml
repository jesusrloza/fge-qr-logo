version: '3.8'

name: dgtipe

services:
  fge-qr-logo:
    container_name: fge-qr-logo
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - '4173:4173'
    restart: unless-stopped
    env_file:
      - ../.env
    environment:
      - NODE_ENV=production
